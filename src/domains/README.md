# Domains Layer

This directory hosts domain-driven modules.

Current migration status:
- `clients` domain scaffold is in place.
- Legacy implementations in `src/views/*` and `src/contexts/*` remain the runtime source during transition.
- All changes are compatibility-first and behavior-preserving.
